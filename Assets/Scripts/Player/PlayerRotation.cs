using UnityEngine;
using System.Collections.Generic;

public class PlayerRotation : MonoBehaviour
{
    //[SerializeField] private List<Transform> transformsToLookAtZeroZero;

    //private void Start()
    //{
    //    if (transformsToLookAtZeroZero == null)
    //    {
    //        Debug.LogError("Transforms to look at zero zero is not assigned.");
    //    }

    //    for (int i = 0; i < transformsToLookAtZeroZero.Count; i++)
    //    {
    //        if (transformsToLookAtZeroZero[i] == null)
    //        {
    //            Debug.LogError($"Transform at index {i} is not assigned.");
    //        }
    //        transformsToLookAtZeroZero[i].rotation = Quaternion.LookRotation(Vector3.zero - transformsToLookAtZeroZero[i].position, Vector3.up); 
    //    }
    //}
}